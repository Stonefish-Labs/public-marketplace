# Facebook Reel Downloader

Download public Facebook reels and videos to local MP4 files.

This project is tool-agnostic and can be used with opencode, claude code, roo, codex, cursor, or any similar agent setup.

## Prerequisite

- `ffmpeg`

Install `ffmpeg`:

- macOS: `brew install ffmpeg`
- Ubuntu/Debian: `sudo apt install ffmpeg`
- Windows: `winget install ffmpeg`

## Details

For usage, supported URL formats, and agent instructions, see `skills/fb-reel-downloader/SKILL.md`.
